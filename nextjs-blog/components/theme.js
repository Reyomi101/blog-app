// Material UI imports
import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#2196f3',
		},
	},
	background: {
		default: '#fff',
	},
});

export default theme;
